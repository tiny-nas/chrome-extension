// 锦盒 TinyNAS 推送助手 — background service worker (MV3, stub)
// Task 4.1: 空实现占位。Task 4.2 将在此实现右键菜单 + nats.ws 推送逻辑。

chrome.runtime.onInstalled.addListener(() => {
  console.log('[tinynas] service worker installed (stub)');
});

chrome.runtime.onStartup.addListener(() => {
  console.log('[tinynas] service worker startup (stub)');
});
